const form=document.getElementById('contact-form');const statusMessage=document.getElementById('statusMessage');const btn=document.getElementById('enviar-btn');form.addEventListener('submit',function(event){event.preventDefault();btn.disabled=!0;const formData=new FormData(form);const name=formData.get('name')
    const email=formData.get('email')
    const message=formData.get('message')
    const data={name:name,email:email,message:message};setTimeout(()=>{statusMessage.textContent='Enviando...';fetch('https://jko-backend.vercel.app/send-email',{method:'POST',headers:{'Content-Type':'application/json',},body:JSON.stringify(data)}).then(response=>{if(!response.ok){throw new Error('Erro ao enviar o formulario')}
    return response.json()}).then(data=>{statusMessage.textContent="O formulario foi enviado com sucesso!";form.reset()}).catch(error=>{console.error('Erro:',error);statusMessage.textContent="Houve um erro ao enviar o formulario. Tente novamente ou entre em contato pelo WhatsApp."});btn.disabled=!1},2000)})